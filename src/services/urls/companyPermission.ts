const baseUrl = 'companyPerm';

const companyPermissionsUrls = {
    list: `${baseUrl}/byCompany/`,
    userPermissions: `${baseUrl}/byUserCompany/`,
    create: `${baseUrl}/`,
    editUserPerm: `${baseUrl}/editUserPerm/`
};

export default companyPermissionsUrls;
