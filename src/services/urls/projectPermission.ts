const baseUrl = 'projPermissions';

const projectUrls = {
    create: `${baseUrl}/create`,
    edit: `${baseUrl}/create`,
    delete: `${baseUrl}/create`,
    list: `${baseUrl}/listByProject`,
    getUserPerm: `${baseUrl}/listByUser`,
    chabgeUserPerm: `companyPerm/EditUserPerm`,
};

export default projectUrls;
