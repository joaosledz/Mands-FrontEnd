const sessionUrl = 'session';

const sessionUrls = {
    create: `${sessionUrl}/Create/`,
    delete: `${sessionUrl}/Delete/`,
    update: `${sessionUrl}/Update/`,
    updatePosition: `${sessionUrl}/UpdatePosition/`,
};

export default sessionUrls;
